/*
 Do not modify, auto-generated by classes.py

 Copyright 2019 Alain Dargelas

 Licensed under the Apache License, Version 2.0 (the "License");
 you may not use this file except in compliance with the License.
 You may obtain a copy of the License at

 http://www.apache.org/licenses/LICENSE-2.0

 Unless required by applicable law or agreed to in writing, software
 distributed under the License is distributed on an "AS IS" BASIS,
 WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 See the License for the specific language governing permissions and
 limitations under the License.
 */

/*
 * File:   cover_group.h
 * Author:
 *
 * Created on December 14, 2019, 10:03 PM
 */

#ifndef UHDM_COVER_GROUP_H
#define UHDM_COVER_GROUP_H

#include <uhdm/sv_vpi_user.h>
#include <uhdm/uhdm_vpi_user.h>

#include <uhdm/containers.h>
#include <uhdm/any.h>

namespace uhdm {
class CoverGroup final : public Any {
  UHDM_IMPLEMENT_RTTI(CoverGroup, Any)

public:
  static constexpr UhdmType kUhdmType = UhdmType::CoverGroup;

  explicit CoverGroup(Serializer *serializer) : basetype_t(serializer) {}
  CoverGroup(Serializer *serializer, const CoverGroup& rhs);
  ~CoverGroup() override = default;
  CoverGroup(const CoverGroup& rhs) = delete;
  CoverGroup& operator=(const CoverGroup& rhs) = delete;

  std::string_view getName() const override;
  bool setName(std::string_view name);
  Identifier* getNameObj() { return m_name; }
  const Identifier* getNameObj() const { return m_name; }
  template <typename T> T* getNameObj() { return any_cast<T>(m_name); }
  template <typename T> const T* getNameObj() const { return any_cast<T>(m_name); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setNameObj(T, bool = false) = delete;
  bool setNameObj(Identifier* data, bool force = false) {
    if ((m_name == nullptr) || (data == nullptr) || force) {
      m_name = data;
      return true;
    }
    return false;
  }

  std::string_view getFullName() const;
  bool setFullName(std::string_view data);

  AttributeCollection* getAttributes() const { return m_attributes; }
  template<typename T> AttributeCollection* getAttributes(T) = delete;
  AttributeCollection* getAttributes(bool createIfNull);
  bool setAttributes(AttributeCollection* data, bool force = false) {
    if ((m_attributes == nullptr) || (data == nullptr) || force) {
      m_attributes = data;
      return true;
    }
    return false;
  }

  IODeclCollection* getIODecls() const { return m_iODecls; }
  template<typename T> IODeclCollection* getIODecls(T) = delete;
  IODeclCollection* getIODecls(bool createIfNull);
  bool setIODecls(IODeclCollection* data, bool force = false) {
    if ((m_iODecls == nullptr) || (data == nullptr) || force) {
      m_iODecls = data;
      return true;
    }
    return false;
  }

  Any* getCoverageEvent() { return m_coverageEvent; }
  const Any* getCoverageEvent() const { return m_coverageEvent; }
  template <typename T> T* getCoverageEvent() { return any_cast<T>(m_coverageEvent); }
  template <typename T> const T* getCoverageEvent() const { return any_cast<T>(m_coverageEvent); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setCoverageEvent(T, bool = false) = delete;
  bool setCoverageEvent(Any* data, bool force = false) {
    if ((m_coverageEvent == nullptr) || (data == nullptr) || force) {
      m_coverageEvent = data;
      return true;
    }
    return false;
  }

  AnyCollection* getStmts() const { return m_stmts; }
  template<typename T> AnyCollection* getStmts(T) = delete;
  AnyCollection* getStmts(bool createIfNull);
  bool setStmts(AnyCollection* data, bool force = false) {
    if ((m_stmts == nullptr) || (data == nullptr) || force) {
      m_stmts = data;
      return true;
    }
    return false;
  }

  std::string_view getEndLabel() const;
  bool setEndLabel(std::string_view name);
  Identifier* getEndLabelObj() { return m_endLabel; }
  const Identifier* getEndLabelObj() const { return m_endLabel; }
  template <typename T> T* getEndLabelObj() { return any_cast<T>(m_endLabel); }
  template <typename T> const T* getEndLabelObj() const { return any_cast<T>(m_endLabel); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setEndLabelObj(T, bool = false) = delete;
  bool setEndLabelObj(Identifier* data, bool force = false) {
    if ((m_endLabel == nullptr) || (data == nullptr) || force) {
      m_endLabel = data;
      return true;
    }
    return false;
  }

  uint32_t getVpiType() const override { return vpiCoverGroup; }
  const Any* getByVpiName(std::string_view name) const override;
  get_by_vpi_type_return_t getByVpiType(int32_t type) const override;
  vpi_property_value_t getVpiPropertyValue(int32_t property) const override;

  int32_t compare(const Any* other, UhdmComparer* comparer) const override;

  using basetype_t::isFiltered;
  bool isFiltered(const Any *data, int32_t relation) const override;

  void swap(const Any* what, Any* with) override;
  void swap(const AnyMap<Any*>& replacements) override;

  UhdmType getUhdmType() const override { return UhdmType::CoverGroup; }

 protected:
  void onChildAdded(Any* child) override;
  void onChildRemoved(Any* child) override;

protected:
  Identifier* m_name = nullptr;
  SymbolId m_fullName = BadSymbolId;
  AttributeCollection* m_attributes = nullptr;
  IODeclCollection* m_iODecls = nullptr;
  Any* m_coverageEvent = nullptr;
  AnyCollection* m_stmts = nullptr;
  Identifier* m_endLabel = nullptr;
};
}  // namespace uhdm

#endif
