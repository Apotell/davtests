/*
 Do not modify, auto-generated by classes.py

 Copyright 2019 Alain Dargelas

 Licensed under the Apache License, Version 2.0 (the "License");
 you may not use this file except in compliance with the License.
 You may obtain a copy of the License at

 http://www.apache.org/licenses/LICENSE-2.0

 Unless required by applicable law or agreed to in writing, software
 distributed under the License is distributed on an "AS IS" BASIS,
 WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 See the License for the specific language governing permissions and
 limitations under the License.
 */

/*
 * File:   assignment.h
 * Author:
 *
 * Created on December 14, 2019, 10:03 PM
 */

#ifndef UHDM_ASSIGNMENT_H
#define UHDM_ASSIGNMENT_H

#include <uhdm/sv_vpi_user.h>
#include <uhdm/uhdm_vpi_user.h>

#include <uhdm/containers.h>
#include <uhdm/atomic_stmt.h>

namespace uhdm {
class Assignment final : public AtomicStmt {
  UHDM_IMPLEMENT_RTTI(Assignment, AtomicStmt)

public:
  static constexpr UhdmType kUhdmType = UhdmType::Assignment;

  explicit Assignment(Serializer *serializer) : basetype_t(serializer) {}
  Assignment(Serializer *serializer, const Assignment& rhs);
  ~Assignment() override = default;
  Assignment(const Assignment& rhs) = delete;
  Assignment& operator=(const Assignment& rhs) = delete;

  bool getBlocking() const { return m_blocking; }
  bool setBlocking(bool data) {
    m_blocking = data;
    return true;
  }

  Expr* getLhs() { return m_lhs; }
  const Expr* getLhs() const { return m_lhs; }
  template <typename T> T* getLhs() { return any_cast<T>(m_lhs); }
  template <typename T> const T* getLhs() const { return any_cast<T>(m_lhs); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setLhs(T, bool = false) = delete;
  bool setLhs(Expr* data, bool force = false) {
    if ((m_lhs == nullptr) || (data == nullptr) || force) {
      m_lhs = data;
      return true;
    }
    return false;
  }

  Any* getRhs() { return m_rhs; }
  const Any* getRhs() const { return m_rhs; }
  template <typename T> T* getRhs() { return any_cast<T>(m_rhs); }
  template <typename T> const T* getRhs() const { return any_cast<T>(m_rhs); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setRhs(T, bool = false) = delete;
  bool setRhs(Any* data, bool force = false) {
    if ((m_rhs == nullptr) || (data == nullptr) || force) {
      m_rhs = data;
      return true;
    }
    return false;
  }

  DelayControl* getDelayControl() { return m_delayControl; }
  const DelayControl* getDelayControl() const { return m_delayControl; }
  template <typename T> T* getDelayControl() { return any_cast<T>(m_delayControl); }
  template <typename T> const T* getDelayControl() const { return any_cast<T>(m_delayControl); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setDelayControl(T, bool = false) = delete;
  bool setDelayControl(DelayControl* data, bool force = false) {
    if ((m_delayControl == nullptr) || (data == nullptr) || force) {
      m_delayControl = data;
      return true;
    }
    return false;
  }

  EventControl* getEventControl() { return m_eventControl; }
  const EventControl* getEventControl() const { return m_eventControl; }
  template <typename T> T* getEventControl() { return any_cast<T>(m_eventControl); }
  template <typename T> const T* getEventControl() const { return any_cast<T>(m_eventControl); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setEventControl(T, bool = false) = delete;
  bool setEventControl(EventControl* data, bool force = false) {
    if ((m_eventControl == nullptr) || (data == nullptr) || force) {
      m_eventControl = data;
      return true;
    }
    return false;
  }

  RepeatControl* getRepeatControl() { return m_repeatControl; }
  const RepeatControl* getRepeatControl() const { return m_repeatControl; }
  template <typename T> T* getRepeatControl() { return any_cast<T>(m_repeatControl); }
  template <typename T> const T* getRepeatControl() const { return any_cast<T>(m_repeatControl); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setRepeatControl(T, bool = false) = delete;
  bool setRepeatControl(RepeatControl* data, bool force = false) {
    if ((m_repeatControl == nullptr) || (data == nullptr) || force) {
      m_repeatControl = data;
      return true;
    }
    return false;
  }

  uint32_t getVpiType() const override { return vpiAssignment; }
  const Any* getByVpiName(std::string_view name) const override;
  get_by_vpi_type_return_t getByVpiType(int32_t type) const override;
  vpi_property_value_t getVpiPropertyValue(int32_t property) const override;

  int32_t compare(const Any* other, UhdmComparer* comparer) const override;

  using basetype_t::isFiltered;
  bool isFiltered(const Any *data, int32_t relation) const override;

  void swap(const Any* what, Any* with) override;
  void swap(const AnyMap<Any*>& replacements) override;

  UhdmType getUhdmType() const override { return UhdmType::Assignment; }


protected:
  bool m_blocking = false;
  Expr* m_lhs = nullptr;
  Any* m_rhs = nullptr;
  DelayControl* m_delayControl = nullptr;
  EventControl* m_eventControl = nullptr;
  RepeatControl* m_repeatControl = nullptr;
};
}  // namespace uhdm

#endif
