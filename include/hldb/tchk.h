/*
 Do not modify, auto-generated by classes.py

 Copyright 2019 Alain Dargelas

 Licensed under the Apache License, Version 2.0 (the "License");
 you may not use this file except in compliance with the License.
 You may obtain a copy of the License at

 http://www.apache.org/licenses/LICENSE-2.0

 Unless required by applicable law or agreed to in writing, software
 distributed under the License is distributed on an "AS IS" BASIS,
 WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 See the License for the specific language governing permissions and
 limitations under the License.
 */

/*
 * File:   tchk.h
 * Author:
 *
 * Created on December 14, 2019, 10:03 PM
 */

#ifndef UHDM_TCHK_H
#define UHDM_TCHK_H

#include <uhdm/sv_vpi_user.h>
#include <uhdm/uhdm_vpi_user.h>

#include <uhdm/containers.h>
#include <uhdm/any.h>

namespace uhdm {
class Tchk final : public Any {
  UHDM_IMPLEMENT_RTTI(Tchk, Any)

public:
  static constexpr UhdmType kUhdmType = UhdmType::Tchk;

  explicit Tchk(Serializer *serializer) : basetype_t(serializer) {}
  Tchk(Serializer *serializer, const Tchk& rhs);
  ~Tchk() override = default;
  Tchk(const Tchk& rhs) = delete;
  Tchk& operator=(const Tchk& rhs) = delete;

  Module* getModule() { return m_module; }
  const Module* getModule() const { return m_module; }
  template <typename T> T* getModule() { return any_cast<T>(m_module); }
  template <typename T> const T* getModule() const { return any_cast<T>(m_module); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setModule(T, bool = false) = delete;
  bool setModule(Module* data, bool force = false) {
    if ((m_module == nullptr) || (data == nullptr) || force) {
      m_module = data;
      return true;
    }
    return false;
  }

  Expr* getDelay() { return m_delay; }
  const Expr* getDelay() const { return m_delay; }
  template <typename T> T* getDelay() { return any_cast<T>(m_delay); }
  template <typename T> const T* getDelay() const { return any_cast<T>(m_delay); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setDelay(T, bool = false) = delete;
  bool setDelay(Expr* data, bool force = false) {
    if ((m_delay == nullptr) || (data == nullptr) || force) {
      m_delay = data;
      return true;
    }
    return false;
  }

  AnyCollection* getExprs() const { return m_exprs; }
  template<typename T> AnyCollection* getExprs(T) = delete;
  AnyCollection* getExprs(bool createIfNull);
  bool setExprs(AnyCollection* data, bool force = false) {
    if ((m_exprs == nullptr) || (data == nullptr) || force) {
      m_exprs = data;
      return true;
    }
    return false;
  }

  TchkTerm* getTchkRefTerm() { return m_tchkRefTerm; }
  const TchkTerm* getTchkRefTerm() const { return m_tchkRefTerm; }
  template <typename T> T* getTchkRefTerm() { return any_cast<T>(m_tchkRefTerm); }
  template <typename T> const T* getTchkRefTerm() const { return any_cast<T>(m_tchkRefTerm); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setTchkRefTerm(T, bool = false) = delete;
  bool setTchkRefTerm(TchkTerm* data, bool force = false) {
    if ((m_tchkRefTerm == nullptr) || (data == nullptr) || force) {
      m_tchkRefTerm = data;
      return true;
    }
    return false;
  }

  TchkTerm* getTchkDataTerm() { return m_tchkDataTerm; }
  const TchkTerm* getTchkDataTerm() const { return m_tchkDataTerm; }
  template <typename T> T* getTchkDataTerm() { return any_cast<T>(m_tchkDataTerm); }
  template <typename T> const T* getTchkDataTerm() const { return any_cast<T>(m_tchkDataTerm); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setTchkDataTerm(T, bool = false) = delete;
  bool setTchkDataTerm(TchkTerm* data, bool force = false) {
    if ((m_tchkDataTerm == nullptr) || (data == nullptr) || force) {
      m_tchkDataTerm = data;
      return true;
    }
    return false;
  }

  Reg* getTchkNotifier() { return m_tchkNotifier; }
  const Reg* getTchkNotifier() const { return m_tchkNotifier; }
  template <typename T> T* getTchkNotifier() { return any_cast<T>(m_tchkNotifier); }
  template <typename T> const T* getTchkNotifier() const { return any_cast<T>(m_tchkNotifier); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setTchkNotifier(T, bool = false) = delete;
  bool setTchkNotifier(Reg* data, bool force = false) {
    if ((m_tchkNotifier == nullptr) || (data == nullptr) || force) {
      m_tchkNotifier = data;
      return true;
    }
    return false;
  }

  int32_t getTchkType() const { return m_tchkType; }
  bool setTchkType(int32_t data) {
    m_tchkType = data;
    return true;
  }

  AttributeCollection* getAttributes() const { return m_attributes; }
  template<typename T> AttributeCollection* getAttributes(T) = delete;
  AttributeCollection* getAttributes(bool createIfNull);
  bool setAttributes(AttributeCollection* data, bool force = false) {
    if ((m_attributes == nullptr) || (data == nullptr) || force) {
      m_attributes = data;
      return true;
    }
    return false;
  }

  uint32_t getVpiType() const override { return vpiTchk; }
  const Any* getByVpiName(std::string_view name) const override;
  get_by_vpi_type_return_t getByVpiType(int32_t type) const override;
  vpi_property_value_t getVpiPropertyValue(int32_t property) const override;

  int32_t compare(const Any* other, UhdmComparer* comparer) const override;

  using basetype_t::isFiltered;
  bool isFiltered(const Any *data, int32_t relation) const override;

  void swap(const Any* what, Any* with) override;
  void swap(const AnyMap<Any*>& replacements) override;

  UhdmType getUhdmType() const override { return UhdmType::Tchk; }

 protected:
  void onChildAdded(Any* child) override;
  void onChildRemoved(Any* child) override;

protected:
  Module* m_module = nullptr;
  Expr* m_delay = nullptr;
  AnyCollection* m_exprs = nullptr;
  TchkTerm* m_tchkRefTerm = nullptr;
  TchkTerm* m_tchkDataTerm = nullptr;
  Reg* m_tchkNotifier = nullptr;
  int32_t m_tchkType = 0;
  AttributeCollection* m_attributes = nullptr;
};
}  // namespace uhdm

#endif
