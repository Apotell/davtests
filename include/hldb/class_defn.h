/*
 Do not modify, auto-generated by classes.py

 Copyright 2019 Alain Dargelas

 Licensed under the Apache License, Version 2.0 (the "License");
 you may not use this file except in compliance with the License.
 You may obtain a copy of the License at

 http://www.apache.org/licenses/LICENSE-2.0

 Unless required by applicable law or agreed to in writing, software
 distributed under the License is distributed on an "AS IS" BASIS,
 WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 See the License for the specific language governing permissions and
 limitations under the License.
 */

/*
 * File:   class_defn.h
 * Author:
 *
 * Created on December 14, 2019, 10:03 PM
 */

#ifndef UHDM_CLASS_DEFN_H
#define UHDM_CLASS_DEFN_H

#include <uhdm/sv_vpi_user.h>
#include <uhdm/uhdm_vpi_user.h>

#include <uhdm/containers.h>
#include <uhdm/scope.h>

namespace uhdm {
class ClassDefn final : public Scope {
  UHDM_IMPLEMENT_RTTI(ClassDefn, Scope)

public:
  static constexpr UhdmType kUhdmType = UhdmType::ClassDefn;

  explicit ClassDefn(Serializer *serializer) : basetype_t(serializer) {}
  ClassDefn(Serializer *serializer, const ClassDefn& rhs);
  ~ClassDefn() override = default;
  ClassDefn(const ClassDefn& rhs) = delete;
  ClassDefn& operator=(const ClassDefn& rhs) = delete;

  bool getVirtual() const { return m_virtual; }
  bool setVirtual(bool data) {
    m_virtual = data;
    return true;
  }

  bool getAutomatic() const { return m_automatic; }
  bool setAutomatic(bool data) {
    m_automatic = data;
    return true;
  }

  int32_t getClassType() const { return m_classType; }
  bool setClassType(int32_t data) {
    m_classType = data;
    return true;
  }

  Extends* getExtends() { return m_extends; }
  const Extends* getExtends() const { return m_extends; }
  template <typename T> T* getExtends() { return any_cast<T>(m_extends); }
  template <typename T> const T* getExtends() const { return any_cast<T>(m_extends); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setExtends(T, bool = false) = delete;
  bool setExtends(Extends* data, bool force = false) {
    if ((m_extends == nullptr) || (data == nullptr) || force) {
      m_extends = data;
      return true;
    }
    return false;
  }

  Implements* getImplements() { return m_implements; }
  const Implements* getImplements() const { return m_implements; }
  template <typename T> T* getImplements() { return any_cast<T>(m_implements); }
  template <typename T> const T* getImplements() const { return any_cast<T>(m_implements); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setImplements(T, bool = false) = delete;
  bool setImplements(Implements* data, bool force = false) {
    if ((m_implements == nullptr) || (data == nullptr) || force) {
      m_implements = data;
      return true;
    }
    return false;
  }

  RefObj* getLibrary() { return m_library; }
  const RefObj* getLibrary() const { return m_library; }
  template <typename T> T* getLibrary() { return any_cast<T>(m_library); }
  template <typename T> const T* getLibrary() const { return any_cast<T>(m_library); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setLibrary(T, bool = false) = delete;
  bool setLibrary(RefObj* data, bool force = false) {
    if ((m_library == nullptr) || (data == nullptr) || force) {
      m_library = data;
      return true;
    }
    return false;
  }

  std::string_view getCell() const;
  bool setCell(std::string_view data);

  RefObj* getConfig() { return m_config; }
  const RefObj* getConfig() const { return m_config; }
  template <typename T> T* getConfig() { return any_cast<T>(m_config); }
  template <typename T> const T* getConfig() const { return any_cast<T>(m_config); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setConfig(T, bool = false) = delete;
  bool setConfig(RefObj* data, bool force = false) {
    if ((m_config == nullptr) || (data == nullptr) || force) {
      m_config = data;
      return true;
    }
    return false;
  }

  TaskFuncCollection* getMethods() const { return m_methods; }
  template<typename T> TaskFuncCollection* getMethods(T) = delete;
  TaskFuncCollection* getMethods(bool createIfNull);
  bool setMethods(TaskFuncCollection* data, bool force = false) {
    if ((m_methods == nullptr) || (data == nullptr) || force) {
      m_methods = data;
      return true;
    }
    return false;
  }

  TaskFuncDeclCollection* getTaskFuncDecls() const { return m_taskFuncDecls; }
  template<typename T> TaskFuncDeclCollection* getTaskFuncDecls(T) = delete;
  TaskFuncDeclCollection* getTaskFuncDecls(bool createIfNull);
  bool setTaskFuncDecls(TaskFuncDeclCollection* data, bool force = false) {
    if ((m_taskFuncDecls == nullptr) || (data == nullptr) || force) {
      m_taskFuncDecls = data;
      return true;
    }
    return false;
  }

  ConstraintCollection* getConstraints() const { return m_constraints; }
  template<typename T> ConstraintCollection* getConstraints(T) = delete;
  ConstraintCollection* getConstraints(bool createIfNull);
  bool setConstraints(ConstraintCollection* data, bool force = false) {
    if ((m_constraints == nullptr) || (data == nullptr) || force) {
      m_constraints = data;
      return true;
    }
    return false;
  }

  ClassDefnCollection* getClassDefns() const { return m_classDefns; }
  template<typename T> ClassDefnCollection* getClassDefns(T) = delete;
  ClassDefnCollection* getClassDefns(bool createIfNull);
  bool setClassDefns(ClassDefnCollection* data, bool force = false) {
    if ((m_classDefns == nullptr) || (data == nullptr) || force) {
      m_classDefns = data;
      return true;
    }
    return false;
  }

  ClassDefnCollection* getDerivedClasses() const { return m_derivedClasses; }
  template<typename T> ClassDefnCollection* getDerivedClasses(T) = delete;
  ClassDefnCollection* getDerivedClasses(bool createIfNull);
  bool setDerivedClasses(ClassDefnCollection* data, bool force = false) {
    if ((m_derivedClasses == nullptr) || (data == nullptr) || force) {
      m_derivedClasses = data;
      return true;
    }
    return false;
  }

  std::string_view getEndLabel() const;
  bool setEndLabel(std::string_view name);
  Identifier* getEndLabelObj() { return m_endLabel; }
  const Identifier* getEndLabelObj() const { return m_endLabel; }
  template <typename T> T* getEndLabelObj() { return any_cast<T>(m_endLabel); }
  template <typename T> const T* getEndLabelObj() const { return any_cast<T>(m_endLabel); }
  template <typename T,
            typename = typename std::enable_if_t<!std::is_base_of_v<Any, std::remove_pointer_t<T>> &&
                                                 !std::is_same<T, std::nullptr_t>::value>>
  bool setEndLabelObj(T, bool = false) = delete;
  bool setEndLabelObj(Identifier* data, bool force = false) {
    if ((m_endLabel == nullptr) || (data == nullptr) || force) {
      m_endLabel = data;
      return true;
    }
    return false;
  }

  uint32_t getVpiType() const override { return vpiClassDefn; }
  const Any* getByVpiName(std::string_view name) const override;
  get_by_vpi_type_return_t getByVpiType(int32_t type) const override;
  vpi_property_value_t getVpiPropertyValue(int32_t property) const override;

  int32_t compare(const Any* other, UhdmComparer* comparer) const override;

  void swap(const Any* what, Any* with) override;
  void swap(const AnyMap<Any*>& replacements) override;

  UhdmType getUhdmType() const override { return UhdmType::ClassDefn; }

 protected:
  void onChildAdded(Any* child) override;
  void onChildRemoved(Any* child) override;

protected:
  bool m_virtual = false;
  bool m_automatic = false;
  int32_t m_classType = 0;
  Extends* m_extends = nullptr;
  Implements* m_implements = nullptr;
  RefObj* m_library = nullptr;
  SymbolId m_cell = BadSymbolId;
  RefObj* m_config = nullptr;
  TaskFuncCollection* m_methods = nullptr;
  TaskFuncDeclCollection* m_taskFuncDecls = nullptr;
  ConstraintCollection* m_constraints = nullptr;
  ClassDefnCollection* m_classDefns = nullptr;
  ClassDefnCollection* m_derivedClasses = nullptr;
  Identifier* m_endLabel = nullptr;
};
}  // namespace uhdm

#endif
